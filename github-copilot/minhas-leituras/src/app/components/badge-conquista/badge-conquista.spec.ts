
// Quero começar a escrever testes unitários para o meu componente BadgeConquistaComponent.

// Este componente é *standalone*, usa o `CommonModule` e injeta o `LeituraService`. 

// A lógica dele calcula se o usuário é se forem lidos 10 ou mais livros, usando um Observable `totalConcluidos` que vem do serviço injetado.

// Por favor, me guie na primeira etapa da configuração do teste, focando no *mock* do `LeituraService`.